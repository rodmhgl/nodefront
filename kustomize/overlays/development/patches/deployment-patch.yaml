kind: Deployment
apiVersion: apps/v1
metadata:
  name: myapp
spec:
  template:
    metadata:
      labels:
        SDLC: development
    spec:
      containers:
        - name: myapp
          env:
            - name: ENVIRONMENT
              value: development
            - name: BG_COLOR
              value: white
            - name: FONT_COLOR
              value: black
      volumes:
        - name: myapp-secret-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: dev-myapp-secret-provider-class