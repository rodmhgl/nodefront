kind: Deployment
apiVersion: apps/v1
metadata:
  name: myapp
spec:
  template:
    metadata:
      labels:
        SDLC: staging
    spec:
      containers:
        - name: myapp
      volumes:
        - name: myapp-secret-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: staging-myapp-secret-provider-class